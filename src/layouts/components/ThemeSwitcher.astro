---
const { className }: { className?: string } = Astro.props;
---

<!-- Lite theme - no theme switcher -->
<div class={`theme-switcher ${className}`}>
  <!-- Theme switcher removed for lite theme -->
</div>
